# Vite загружает .env из корня монорепо (envDir в vite.config).
# VITE_API_URL задаётся в корневом .env (рядом с TELEGRAM_BOT_TOKEN, DATABASE_URL).
# Пример: VITE_API_URL=http://localhost:3000 или URL туннеля на бэкенд.
